{
  "skill": "{{skill-slug}}",
  "version": "0.1.0",
  "source_book": "{{BOOK_TITLE}} — {{AUTHOR}}",
  "darwin_compatible": true,
  "test_cases": [
    {
      "id": "should-trigger-01",
      "type": "should_trigger",
      "prompt": "{{User's typical phrase 1 — positive scenario}}",
      "expected_behavior": "Should activate {{skill-slug}} and {{expected specific action}}",
      "notes": "{{Why this is a positive scenario}}"
    },
    {
      "id": "should-trigger-02",
      "type": "should_trigger",
      "prompt": "{{User's typical phrase 2}}",
      "expected_behavior": "...",
      "notes": "..."
    },
    {
      "id": "should-trigger-03",
      "type": "should_trigger",
      "prompt": "{{User's typical phrase 3}}",
      "expected_behavior": "...",
      "notes": "..."
    },
    {
      "id": "should-not-trigger-01",
      "type": "should_not_trigger",
      "prompt": "{{Bait 1 — seems related but shouldn't be called}}",
      "expected_behavior": "Should not activate this skill, because {{reason}}",
      "notes": "Bait: {{What type of bait this is}}"
    },
    {
      "id": "should-not-trigger-02",
      "type": "should_not_trigger",
      "prompt": "{{Bait 2 — another case that shouldn't be called}}",
      "expected_behavior": "...",
      "notes": "..."
    },
    {
      "id": "edge-01",
      "type": "edge_case",
      "prompt": "{{Ambiguous boundary scenario}}",
      "expected_behavior": "{{Expected reasonable judgment — may call, may not call, but must explain why}}",
      "notes": "Boundary: {{Why this is a boundary case}}"
    }
  ],
  "minimum_pass_rate": 0.8,
  "notes": "At least 3 should_trigger + 2 should_not_trigger + 1 edge_case. All should_not_trigger must pass (bait tolerance is 0)."
}
